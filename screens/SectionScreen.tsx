import { SectionContainer, SectionText } from '@styles/MainPageStyles';
import React from 'react';

type Props = {};

const SectionScreen = () => {
	return (
		<SectionContainer>
			<SectionText>Section Screen</SectionText>
		</SectionContainer>
	);
};

export default SectionScreen;
